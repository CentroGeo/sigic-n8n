
services:
  ollama-proxy:
    image: ollama-proxy:local
    build:
      context: .
    container_name: ollama-proxy
    env_file:
      - .env
    ports:
      - "11434:11434"
    restart: unless-stopped