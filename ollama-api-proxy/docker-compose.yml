
services:
  proxy:
    build: .
    env_file:
      - .env
    environment:
      MODELS_CONFIG: /app/models.json
    volumes:
      - ./models.json:/app/models.json:ro
    ports:
      - "11434:11434"
    restart: unless-stopped
